function test_matrices() {
    const A = [
        [1, 2, 3],
        [4, 5, 6]
    ];
}

function test_symbols() {
    let alpha = 1;
    let beta = 2;
    let integral = 0;
    let infinity = 999;
    let x_1 = 5;
    
    if (alpha !== beta && alpha <= 10 || beta >= 2) {
        // do something
    }
}\n