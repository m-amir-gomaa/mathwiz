// JavaScript test file for MathViz.nvim

function testMatrices() {
    const A = [
        [1, 2, 3],
        [4, 5, 6],
        [7, 8, 9]
    ];
}

function testSymbols() {
    let alpha = 1;
    let beta = 2;
    
    if (alpha !== beta) {
        console.log("neq");
    }
    if (alpha <= 10) {}
    if (beta >= 2) {}
    
    // => operator
    const fn = (x) => x * 2;
}

    // Advanced math symbols
    let subset = 1;
    let union = 2;
    let infinity = 3;
    let integral = 4;
    let x_1 = 5; // subscript
    let x_i = 6;
    let x_2 = 7;
    let sum = 8;
    let and_op = (subset && union); // logic
