// JavaScript test file for MathViz.nvim

function testMatrices() {
    const A = [
        [1, 2, 3],
        [4, 5, 6],
        [7, 8, 9]
    ];
}

function testSymbols() {
    let alpha = 1;
    let beta = 2;
    
    if (alpha !== beta) {
        console.log("neq");
    }
    if (alpha <= 10) {}
    if (beta >= 2) {}
    
    // => operator
    const fn = (x) => x * 2;
}
