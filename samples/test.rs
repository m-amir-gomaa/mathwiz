// Rust test file for MathViz.nvim

fn test_matrices() {
    let A = [
        [1, 2, 3],
        [4, 5, 6],
        [7, 8, 9]
    ];
}

fn test_symbols() {
    let alpha = 1;
    let beta = 2;
    
    if alpha != beta {
        println!("neq");
    }
    if alpha <= 10 {}
    if beta >= 2 {}
    
    // -> return type operator
    // fn foo() -> i32 {}
}
