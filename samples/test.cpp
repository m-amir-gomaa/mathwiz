#include <vector>
#include <iostream>

void test_matrices() {
    std::vector<std::vector<int>> A = {
        {1, 2, 3},
        {4, 5, 6}
    };
}

void test_symbols() {
    int alpha = 1;
    int beta = 2;
    int integral = 0;
    int infinity = 999;
    int x_1 = 5;
    
    if (alpha != beta && alpha <= 10 || beta >= 2) {
        // do something
    }
}\n